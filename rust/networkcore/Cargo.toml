[package]
name = "networkcore"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "net", "time", "sync", "macros"] }
dashmap = "6"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
